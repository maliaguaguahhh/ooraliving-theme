{% comment %}
  Disabled artificial LCP placeholder.

  The previous markup rendered a huge off-screen `.xo-lcp` text node that
  Lighthouse selected as the Largest Contentful Paint element on mobile.
  Keep this snippet empty so existing `{% render 'lcp' %}` calls stay safe
  while the browser measures the real above-the-fold content instead.
{% endcomment %}
