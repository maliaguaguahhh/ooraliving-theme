{% comment %}
  Renders a sort by field for a collection or search object

  Accepts:
  - name: {String} Name of the field (optional)
  - value: {String} Value of the field (optional)
  - label: {String} Label of the field (optional)
  - checked: {Boolean} Whether the field is checked (optional)

  Usage:
  {% render 'field-select-custom-option' %}
{% endcomment %}

<label class="xo-field-select-custom__check" tabindex="0" role="button" aria-label="{{ label | escape }}">
  <input
    type="radio"
    class="xo-field-select-custom__input"
    name="{{ name }}"
    arial-label="{{ label | escape }}"
    xo-label="{{ label | escape }}"
    value="{{ value | escape }}"
    {% if use_with == 'product-variant' %}
      xo-option-value-id="{{ option_value_id }}"
      xo-variant-id="{{ variant_id }}"
      {% if value.product_url != blank %}
        xo-connected-product-url="{{ shop.url }}{{ value.product_url }}"
      {% endif %}
      {% if value.available == false and variant_id == blank %}
        xo-unavailable
      {% endif %}
      {% if variant.available == false and variant_id != blank %}
        xo-disabled
      {% endif %}
    {% endif %}
    {% if checked %}
      checked
    {% endif %}
  >
  <span class="xo-field-select-custom__icon">
    {% render 'icon', name: 'check', size: '2.4rem' %}
  </span>
  <span class="xo-field-select-custom__label">{{ label | escape }}</span>
</label>
